/**
 * RetryButton
 * Re-export de ErrorMessage para compatibilidad con imports existentes.
 * El botón de reintento vive integrado dentro de ErrorMessage.jsx.
 */
export { default } from "./ErrorMessage";
